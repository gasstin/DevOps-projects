pipeline {
    agent none 
    stages {
        stage('Check tools') { 
            agent {
                docker {
                    image 'python:3.10-alpine'
                }
                
            steps {
                sh 'docker --version'
                sh 'python3 --version'
                sh 'make --version'
                }
        }
        }
    }
}

